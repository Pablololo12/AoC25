(define-module (days day06)
  #:export (run))

(define (part1 input)
  "todo")

(define (part2 input)
  "todo")

(define (run input)
  (display "Part1: ")
  (display (part1 input))
  (newline)
  (display "Part2: ")
  (display (part2 input))
  (newline))
